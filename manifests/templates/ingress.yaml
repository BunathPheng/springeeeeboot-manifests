---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springeeeeboot                  # Ingress name
  labels:
    app: springeeeeboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springeeeeboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springeeeeboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
